#include <stdio.h>

int main() {
    int m;
     printf("enter the number: ");
     scanf("%d",&m);
     
     if(m>=-1 || m<=1){
      printf("your value is valid: ");
     }
     else("your value is not valid: ");
    return 0;
}